spring:
  profiles: mariadb
  datasource:
    url: jdbc:tc:mariadb:10.3://localhost:3306/dbs

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDB103Dialect
backbase:
  db:
    cleanup: db/sql/mysql/cleanupTables.sql

