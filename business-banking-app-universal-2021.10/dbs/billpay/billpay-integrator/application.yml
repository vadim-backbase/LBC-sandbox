---
### Live profile
spring:
  profiles: live
  datasource:
    driver-class-name: ${spring.datasource.driver-class-name}
    username: ${spring.datasource.username.billpay-integrator}
    password: ${spring.datasource.password.billpay-integrator}
    url: ${spring.datasource.url.billpay-integrator}

